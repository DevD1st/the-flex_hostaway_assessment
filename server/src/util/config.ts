export const HOSTAWAY_LISTING_URL = "https://api.hostaway.com/v1/listings";
export const HOSTAWAY_REVIEWS_URL = "https://api.hostaway.com/v1/reviews";
/**
 * Should be used as bearer token by frontend
 * when sending request to this API as admin
 * I didn't put this in .env as it would require
 * another package to manage which is vert unnecessary.
 */
export const API_ADMIN_TOKEN = "admin";
